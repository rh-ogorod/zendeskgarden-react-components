import { Meta, ArgsTable, Canvas, Story } from '@storybook/addon-docs';
import { useArgs } from '@storybook/client-api';
import { MultiThumbRange, Fieldset, Field } from '@zendeskgarden/react-forms';
import { MultiThumbRangeStory } from './stories/MultiThumbRangeStory';
import { hintArgs, hintArgTypes, messageArgs, messageArgTypes } from './stories/common';

<Meta
  title="Packages/Forms/MultiThumbRange"
  component={MultiThumbRange}
  subcomponents={{ 'Fieldset.Legend': Fieldset.Legend, Field }}
/>

# API

<ArgsTable />

# Demo

<Canvas>
  <Story
    name="MultiThumbRange"
    args={{
      legend: 'Legend',
      ...hintArgs,
      ...messageArgs
    }}
    argTypes={{
      legend: { name: 'children', table: { category: 'Legend' } },
      ...hintArgTypes,
      ...messageArgTypes
    }}
    parameters={{
      design: {
        allowFullscreen: true,
        type: 'figma',
        url: 'https://www.figma.com/file/6g87L4FdKZTA3knt3Rsfdx/Garden?node-id=103%3A20263'
      }
    }}
  >
    {args => {
      const updateArgs = useArgs()[1];
      const handleChange = ({ minValue, maxValue }) => updateArgs({ minValue, maxValue });
      return <MultiThumbRangeStory {...args} onChange={handleChange} />;
    }}
  </Story>
</Canvas>
